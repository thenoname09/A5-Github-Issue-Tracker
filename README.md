


- Create a readme file and answer this question on your own. Don’t copy-paste from Google or any AI chatbot. 
    - 1️⃣ What is the difference between var, let, and const?
    var - var is Old model and function-scoped and same bug issue
    let - Can reassign the value 
    const- Cannot reassign the value
    
    - 2️⃣ What is the spread operator (...)?
    spread operator allow to expand arrays, objects

    - 3️⃣ What is the difference between map(), filter(), and forEach()?
      map() transform each element in an array and create a new array 
      filter() using  for elements matching condition and return new arrays
      forEach() execute a provided function once for each element in an array.
    - 4️⃣ What is an arrow function?
      =>
    - 5️⃣ What are template literals?
    JavaScript ES6 feature using backticks (` `) write multi-line strings/html-code without any problem 






